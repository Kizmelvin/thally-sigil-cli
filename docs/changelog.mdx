---
title: Changelog
description: Release history for sigil-cli.
---

# Changelog

## v0.1.0

- `sigil verify` — HMAC-SHA256 signature verification
- `sigil keygen` — webhook secret generation
- `sigil inspect` — delivery retry schedule output
- Pipe support for `--payload` via stdin
- JSON output mode for `sigil inspect`
